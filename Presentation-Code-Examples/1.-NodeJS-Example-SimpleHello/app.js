// Simple example that we run in the terminal.
function sayHowDoYaDo(name) {
    console.log('How do ya do, ' + name + "? Nice day out eh?");
}

sayHowDoYaDo("THAT Conference"); // Pass in a parameter to execute function. Can run "node app.js" to execute program.
//console.log(window); // Do not have the window or document objects. These are part of the browser.